#ifndef GRADE_HPP
#define GRADE_HPP

namespace grade {

bool is_higher(unsigned int a, unsigned int b);
bool is_lower(unsigned int a, unsigned int b);
unsigned int increment(unsigned int grade);
unsigned int decrement(unsigned int grade);

} // namespace grade

#endif
